$('.port-item').click(function () {
    $('.collapse').collapse('hide');
  });

  var typed = new Typed(".typing", {
      strings: ["Developer", "Teacher"],
      typeSpeed: 100,
      backSpeed: 60,
      loop: true
  });